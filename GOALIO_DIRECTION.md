# Goalio 产品方向与首版约束

> 状态：第一版建站基线  
> 日期：2026-09-06  
> 平台：手机网页  
> 上线目标：3 天内交付可供首批内部用户使用的版本

## 1. 产品目标

Goalio 帮助用户在维持正常生活的前提下，知道自己每天有多少钱可以安全地归入一个攒钱目标，并判断额外消费会怎样影响目标完成时间。

用户只需要完成两类动作：

1. 首次使用时填写确定的未来收支和一个攒钱目标。
2. 此后每天输入一次当前真实余额。

系统负责回答：

- 今天为目标增加或回撤了多少钱？
- 当前已经安全攒下多少钱，进度是多少？
- 按当前情况预计何时完成？
- 明天需要预留多少基础生活费？
- 在保护当前目标进度并仍能按目标日期完成的前提下，明天额外最多可以花多少？
- 一笔大额消费会不会影响目标，会延迟多久？
- 如果有影响，降低到多少钱可以消除影响？
- 如果坚持原金额，最早延后到哪一天购买可以消除影响？

## 2. 第一性原则

Goalio 的核心价值是减少用户的计算、预测和决策负担。任何设计与功能都应直接服务于以下闭环：

`首次设置 → 每日输入真实余额 → 获得可理解的目标进度 → 评估消费决策`

以下原则拥有最高优先级：

1. 今日真实余额是现实资金的唯一真值。
2. 当天已经发生的收入和支出均包含在余额中，系统只从明天开始预测未来现金流。
3. 所有资金属于同一个真实资金池；“已攒金额”是一种安全软锁定，不代表实际转账或分账。
4. 基础生活与确定支出的资金安全优先于攒钱目标，攒钱目标优先于自由消费。
5. `UNKNOWN` 与确定为 `0` 必须严格区分。未来信息不足时明确告知用户，禁止自行补零。
6. `SafeGoalCapacity` 表示当前余额扣除必要预留后，可以安全归入目标的金额。
7. `EffectiveGoalSaved` 等于当前 `SafeGoalCapacity`，并且始终满足：
   `0 ≤ EffectiveGoalSaved ≤ SafeGoalCapacity ≤ GoalAmount`。
8. 当前余额足以覆盖必要预留和完整目标时，目标当天完成。
9. 同一天重复输入相同余额得到相同进度；余额变化时立即按完整当前输入重新计算。
10. 历史结果只用于解释本次变化，不参与限制当前目标分配。
11. 每日自由消费额度不做机械结转；未消费金额通过下一次更高的真实余额自然进入重新计算。
12. 所有消费影响均通过同一套逐日未来模拟器重新计算，禁止用“消费金额 ÷ 每日攒钱额”推算延期。
13. 达到 100% 后，在用户确认购买目标前继续保护目标资金。
14. 用户确认购买时只接收购买后的最新真实余额，系统不再扣减一次目标金额。

## 3. 最重要的产品体验

Goalio 把当前余额依次分给必要生活和固定支出、当前目标、自由消费。未来需要预留的生活费用减少后，目标分配可能增加；余额或计划变化后，三个资金层级会一起重新计算。

第一版必须让用户看到类似以下体验：

| 日期 | 用户输入的真实余额 | Goalio 展示的目标进度 |
| --- | ---: | ---: |
| 第 1 天 | ¥1,000 | ¥600 |
| 第 2 天 | ¥970 | ¥610 |
| 第 3 天 | ¥940 | ¥620 |
| 第 4 天 | ¥910 | ¥630 |

具体金额由真实余额、未来现金流和必要预留计算。这个表用于定义体验方向，不作为固定计算结果。当前存在大量安全资金时，全部安全容量立即显示为已为目标留好；达到目标金额后当天完成。

## 4. 第一版用户流程

### 4.1 邀请码进入

- 每位内部用户拥有不同的邀请码。
- 邀请码由服务端校验，不写入前端代码。
- 邀请码首次激活后绑定当前浏览器设备。
- 换设备或清理浏览器数据后，需要由管理端重置邀请码。
- 服务端只保存邀请码哈希、激活状态、设备绑定标识和必要时间戳。
- 用户的余额、收入、支出、订阅和目标数据不上传服务端。

### 4.2 首次设置

用户按四步填写：

1. 生活费到账规则：支持每周、每月、每季度、每半年及一次性收入，并可填写起止日期。
2. 每日计划基础饮食费用。
3. 固定订阅和已确认支出：名称、金额、扣费周期、扣费日期、起止日期。
4. 一个攒钱目标：目标名称、目标金额、完成截止日期。

完成设置后，用户立即输入一次当前真实余额，系统按必要预留和目标金额计算当前 `EffectiveGoalSaved`。

### 4.3 每日首页

首页只要求用户输入当前真实余额，然后展示：

- 目标名称
- 已攒金额 / 目标金额
- 当前进度百分比
- 今日增加或回撤金额
- 今日进度变化百分比
- 预计完成日期，或“未来信息不足 / 当前预测范围内无法完成”
- 明日基础生活费用
- 明日额外可花上限；当前计划无法按期完成时不显示金额
- 大额消费评估入口

### 4.4 大额消费评估

用户输入消费名称和金额后，系统输出以下结果之一：

- 可以购买，预计不会拖慢当前计划。
- 会拖慢若干天，但仍可在目标截止日前完成。
- 会导致或加重延期，并展示新的预计完成日期。
- 会造成未来基础生活或固定支出的现金流缺口。
- 当前未来信息不足，无法可靠计算具体延期。
- 消费后目标在可靠预测范围内无法完成。

只要能够可靠计算，页面同时给出两种方案：

1. 降低预算：今天最多花多少钱可以完全不拖慢当前预计完成日期。
2. 延后购买：坚持原金额时，最早在哪一天购买可以完全不拖慢当前预计完成日期。

## 5. 算法实现边界

第一版使用一套统一的逐日模拟器 `RunSimulation()`，首页预测、明日额外可花上限和大额消费评估都调用它，避免出现多套口径。

核心计算链为：

`真实余额 → 未来确定现金流 → RequiredReserve → SafeGoalCapacity → EffectiveGoalSaved → BaselineCompletionDate`

额外消费链为：

`在指定日期加入消费 → 从今天重新逐日模拟 → 校验未来必要预留 → ScenarioCompletionDate → 与 BaselineCompletionDate 比较 → 输出影响与替代方案`

消费评估必须先保护生活与固定支出，再比较目标完成日期：

- 今日可保持当前目标进度的消费上限为：
  `SafeNoDelayLimit = max(0, CurrentBalance - RequiredReserve - EffectiveGoalSaved)`。
- 今日消费后的剩余余额低于 `RequiredReserve` 时，结果优先归类为未来现金流缺口，并展示缺口金额。
- “降低预算”的最高金额不得超过 `SafeNoDelayLimit`；日期二分搜索只能在该安全上限内进行。
- “延后购买”的每个候选日期都必须同时满足未来必要预留充足、预计完成日期不晚于基线日期。
- 首页“明天额外最多可花”通过整数二分搜索得到。每个候选金额都作为明日消费重新模拟，只有未来必要预留充足、模拟后的 `EffectiveGoalSaved` 不低于当前目标进度，并且预计完成日期不晚于目标截止日时才可采用。
- `RequiredReserve` 已经包含明日生活费和明日到期的固定支出，计算额外可花上限时不得再次直接扣除同一笔明日现金流。
- 当前计划已经无法按目标截止日完成时，首页不得展示可花金额，应引导用户调整安排。

算法实现必须满足：

- 金额统一使用整数分，禁止使用普通浮点数计算人民币。
- 预测范围、可靠预测终点、安全观察期和完成日期搜索边界按既定算法执行。
- 未来收入、生活成本和固定支出按日期展开，未知状态保留到计算结果与页面文案。
- `RequiredReserve` 通过未来现金流倒推。
- 购买金额不得侵占 `RequiredReserve`；完成日期按整天取整产生的余量不得覆盖资金缺口。
- 未来日期购买方案从今天重新模拟，并逐日扫描候选购买日期。
- 金额上限可以在满足单调性的前提下使用整数二分搜索。
- 每次修改目标或资金规则都视为重新规划，并以完整当前输入重算；历史值仅用于展示变化。

完整公式与状态更新规则以用户提供的《算法》原文为实现依据；若方向文件与详细公式出现冲突，以资金安全、更少假设、真实余额优先和统一资金瀑布四项原则判断，并在开发前明确记录修订。

## 6. 网站技术方向

第一版采用单一 Next.js 项目：

- 手机优先的响应式网页，可添加到手机主屏幕。
- 页面与邀请码接口由同一项目提供。
- 用户财务数据保存在浏览器本地存储中。
- 邀请码及设备绑定信息存放在服务端数据库中。
- 正式环境全站使用 HTTPS。
- 核心算法写成独立、无界面的 TypeScript 模块，页面、Demo 和自动化测试共用。
- 日期、金额和周期规则使用明确的数据结构，所有计算统一使用用户本地时区的自然日。

## 7. Demo 与正式体验隔离

Demo 中提供一个日期控制台，用来前进或后退日期，并快速验证连续多日的余额和目标变化。

强制约束：

- 模拟日期只能通过开发或 Demo 构建开关启用。
- 正式环境不显示控制台入口。
- 正式环境所有业务日期始终来自系统当前自然日。
- 核心算法接收显式的 `today` 参数，方便测试；正式页面只能由受控时间服务传入真实日期。
- 构建与上线检查必须包含“正式环境无法改变日期”。

## 8. 三天首版范围

### 必须完成

- 独立邀请码校验、首次激活和设备绑定
- 四步首次设置
- 当前真实余额录入
- 当前输入驱动的即时增加与余额恶化回撤
- 当前进度和预计完成日期
- 明日基础生活费与额外可花上限
- 大额消费影响分类
- 无影响最高购买金额
- 最早无影响购买日期
- 设置修改与重新规划
- 本地数据持久化
- Demo 日期控制台
- 手机端完整可用
- 上线前的核心算法自动化测试

### 暂缓

- 用户账号、短信和第三方登录
- 多设备同步与云端财务数据
- 多个同时进行的目标
- 银行卡或支付平台自动同步
- 消费流水记录与分类账
- 社交、排行榜和公开分享
- 原生 App 与小程序
- 复杂图表、AI 对话和个性化推荐

任何新增需求都要先回答：它是否直接改善“每日输入余额后理解目标进度”或“评估额外消费”这两个核心任务？若答案不明确，第一版不加入。

## 9. 首版完成标准

第一版达到可用状态需要同时满足：

1. 新用户可凭独立邀请码在手机上完成设置并看到首页结果。
2. 同一天重复输入相同余额得到相同目标进度。
3. 连续推进日期并输入逐日下降的合理余额时，目标进度可以按照安全容量与节奏逐日增长。
4. 余额突然下降时，进度会被安全容量限制并允许回撤。
5. 未来存在固定扣费时，系统会提前预留资金。
6. 未来数据出现未知时，页面显示信息不足，计算过程不会把未知当作零。
7. 大额消费评估能输出影响、延期天数和两类替代方案。
8. 正式网站内无法使用日期控制台。
9. 刷新页面后本地数据仍然存在。
10. 服务端看不到用户的具体财务数据。
11. 核心算法边界场景通过自动化测试。
12. 在常见手机尺寸下，首次设置、每日录入和消费评估均可单手完成。

## 10. 实施顺序

1. 建立项目骨架、手机端视觉基础和数据模型。
2. 用测试固定金额、日期、周期、未知状态和资金优先级规则。
3. 实现统一逐日模拟器与核心计算链。
4. 接入首次设置、每日首页和本地持久化。
5. 实现消费影响评估与两种解决方案。
6. 实现邀请码服务和设备绑定。
7. 加入 Demo 日期控制台，并验证正式构建隔离。
8. 完成手机端体验检查、算法回归测试与部署。

## 11. 防偏离检查

每次准备实现功能时，依次检查：

1. 它是否缩短了首次设置到首次获得结果的路径？
2. 它是否让每日余额输入更简单？
3. 它是否提高了目标进度或消费影响的可信度？
4. 它是否保护用户隐私与未来必要支出？
5. 它是否能在三天首版范围内完成并测试？

未通过以上检查的功能，留到首批内部用户反馈后再决定。
